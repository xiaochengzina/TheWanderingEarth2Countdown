# 开发笔记（非用户文档）

这份文件是给**以后要改动这套皮肤的人**（包括未来的我）看的，
记录的是实际调试出来的结论和踩过的坑。发布给用户时可以直接删掉。

---

## 0. 版本号与协议

**当前版本：1.2.0。版本号由作者指定，未经作者许可不要改动。**

**协议：不设协议，也不要写出来。既不要加 CC / MIT / GPL 之类的许可，也不要写上「无协议」这类说明。**

`Version=` 出现在这几处，改的时候必须一起改，否则面板上和皮肤属性里会对不上：

| 文件 | 位置 |
| --- | --- |
| `CountDown.ini` | `[Metadata]` 的 `Version=` |
| `Settings\Settings.ini` | `[Metadata]` 的 `Version=`、左栏副标题、高级页「关于」那一行 |
| `@Resources\Scripts\Settings.lua` | 文件头注释 |

协议字段同理，**应该整行不存在**：

| 文件 | 位置 |
| --- | --- |
| `CountDown.ini` | `[Metadata]` 里没有 `License=` 行 |
| `Settings\Settings.ini` | `[Metadata]` 里没有 `License=` 行 |
| `Settings\Settings.ini` | 高级页「关于」那一行只写「版本 1.2.0 · 作者 小城子」 |
| `README.md` | 文末没有协议章节 |

---

## 0.5 配置名（= 文件夹名）

**就叫 `TheWanderingEarth2Countdown`（27 字符），作者明确决定不改短，未经许可不要改名。**

名字长只是菜单里显示得满一点，Rainmeter 对配置名没有长度限制，不影响功能。
改名的代价却不少：`Rainmeter.ini` 里按配置名存的窗口位置 / 置顶等设置会全部丢失。

万一哪天真的要改，这 **14 处**必须一起改（漏一处面板就找不到主皮肤）：

| 文件 | 位置 | 处数 |
| --- | --- | --- |
| 文件夹名 | `Skins\TheWanderingEarth2Countdown\` | 1 |
| `@Resources\Scripts\Settings.lua` | `local MAIN = 'TheWanderingEarth2Countdown'` ← **最容易漏** | 1 |
| `Settings\Settings.ini` | 打开文件夹按钮的 `LeftMouseUpAction=["#SKINSPATH#TheWanderingEarth2Countdown"]` | 2 |
| `Settings\Settings.ini` | 高级页「关于」那一行、注释、工具提示 | 3 |
| `CountDown.ini` | `ContextAction=[!ActivateConfig "TheWanderingEarth2Countdown\Settings" ...]` | 1 |
| `README.md` | 安装路径与 FAQ 说明 | 5 |
| `Rainmeter.ini` | 旧配置留下的 `[TheWanderingEarth2Countdown]` 段要手工清掉 | — |

改完必须**完整重启 Rainmeter**（`!Quit` 再启动），新配置名才会被重新索引。

---

## 1. 皮肤文件的编码必须是 UTF-16 LE（带 BOM）

Rainmeter **只对 UTF-16 LE 文件里的中文值可靠**。

- 存成 UTF-8（无论有没有 BOM），中文值都会被当成系统 ANSI 码页解码，
  渲染出来就是「璺濆お闃虫唉闂」这样的乱码。
- `.lua` 文件用 UTF-8 **不带 BOM** 即可（那是 Lua 自己的读取路径）。
- 另外：文件**第一行不能是 `[Variables]` / `[Rainmeter]`**。
  Rainmeter 不会跳过文件开头的 BOM，段名会被 BOM 污染而失效
  （表现为「`#BarX#` is unknown」）。所以每个文件都以一段注释开头。

需要批量转换时（把整个皮肤目录转成 UTF-16 LE 或转回 UTF-8 方便编辑）：

```powershell
# -To utf8  /  -To utf16
.\Enc.ps1 -To utf16 -Root <皮肤目录>
```

### 1.1 ⚠ 别为了「改一下」就整棵树翻编码（真出过事故）

曾经为了用编辑器改一行，反复 `Enc.ps1 -To utf8` 改完再 `-To utf16`。
几次来回之后翻车了：

- `Variables.inc` 的 `CnText` 变成一串 `U+FFFD` 替换字符，**内容永久丢失**；
- 5 个 `.ini/.inc` 全被留在 UTF-8 无 BOM 状态，Rainmeter 读出来全是乱码；
- 最后靠 `git diff` 才确认只坏了那一行，其余文件是「编码错、内容好」。

**正确做法：按单个文件、显式指定编码读写，不要动整棵树。**

```powershell
$p = '...\Variables.inc'

# 读：不传编码时 .NET 会靠 BOM 自动识别，所以读可以偷懒
$txt = [IO.File]::ReadAllText($p)

# 写：必须自己管 BOM 和行尾
#   注意 UnicodeEncoding.GetBytes() 不会输出 BOM，只有 GetPreamble() 才有
$enc = New-Object Text.UnicodeEncoding($false, $true)
$out = New-Object Collections.Generic.List[byte]
$out.AddRange($enc.GetPreamble())
$out.AddRange($enc.GetBytes($txt))
[IO.File]::WriteAllBytes($p, $out.ToArray())
```

另外 `-replace '...','..."` 之类的操作很容易顺手把 `\r\n` 变成 `\n`，
写回前记得把行尾统一回 CRLF。

**改完必跑的三项体检**（`.ini` / `.inc` 全部文件）：

1. 前两字节是不是 `FF FE`（UTF-16 LE + BOM）；
2. 全文有没有 `U+FFFD` 替换字符；
3. 行尾是不是全 CRLF（裸 LF 计数为 0）。

---

## 2. Rainmeter 的 Lua 桥接把 Lua 字符串当作 ANSI

- 从 bang 参数或 `SKIN:GetVariable` 拿到的中文**原样传回去是安全的**：
  解码 / 编码对称，来回一趟不会变。
- 但在 Lua 里自己拼出来的中文，再交给 Rainmeter 就会被按 ANSI 重新编码 → 乱码。
  **包括 `.lua` 源码里的中文字面量**：Lua 源码是 UTF-8，它的字节被当成 ANSI 解释，
  所以 `'循环'` 会渲染成「寰口屽」，连 `' · '` 这样的全角符号都会变成「路」。
- 结论：**Lua 里一个非 ASCII 字符都不能出现在字符串字面量里**（注释无所谓）。
  需要中文就放进 `.ini` 的 `[Variables]`，由 Lua 用 `SKIN:GetVariable` 读出来再传回。
  本皮肤的做法：
  - 「恢复默认」用 `[!WriteKeyValue ... "#DefCnText#"]` 这种纯 ASCII bang，由 Rainmeter 展开；
  - 面板的「循环」「每年」「下一次：」等中文全在 `Settings.ini` 的 `[Variables]` 里；
  - 单位文字在 `Layout.inc` 里，Lua 只做数字拼接。

自检方法：找出 `.lua` 里「非注释行含中文」的行，应当为 0 条。

---

## 3. 别用 Lua 读 UTF-16 文件

Windows 文本模式会把 `0x1A` 当成 EOF，而 UTF-16 文件里几乎必然出现
`0x1A` 字节（例如 `U+4E1A`「业」的低字节就是 `1A`），结果只能读到文件开头。
如果将来真要读，用 `io.open(path, 'rb')` 并自己写 UTF-16 → UTF-8 转换。

---

## 4. `os.time` 的字段名是 `min` / `sec`

```lua
os.time{ year=2026, month=9, day=25, hour=18, min=40, sec=0 }         -- 正确
os.time{ year=2026, month=9, day=25, hour=18, minute=40, second=0 }  -- 静默忽略！
```

写成 `minute` / `second` **不会报错**，只会被当成 0 —— 表现是「分钟莫名变成 00」。
排查这类「时间不对但没报错」的问题时先看这里。
（`Common.DaysLeft()` 里也有同样的写法，一并改正过。）

---

## 5. `LeftMouseUpAction` 的字符串很容易写坏

控制面板里所有的点击动作都是 `!CommandMeasure "Script" "函数(...)"`。
一旦括号 / 引号没配平，**点一下就是一个 Lua 语法错误**，
连点几次还能把 Rainmeter 拖崩（`0xc0000005`）。

2026-09 就出过一次：生成 `Settings.ini` 的脚本漏了右括号，
17 条动作全成了 `"OpenTextInput('TargetYear'"`，同时 `$UserInput$`
被 PowerShell 当成变量展开成了 `$`，导致输入框弹出来也拿不到内容。

**改完必须自检**——把 ini 里所有动作字符串取出来，检查：

1. 方括号 `[ ]` 成对；
2. 引号数成对；
3. `(` 与 `)` 数量相等。

用 PowerShell 一把梭：

```powershell
$p = '<皮肤目录>\Settings\Settings.ini'
foreach ($line in [IO.File]::ReadAllLines($p)) {
    if ($line -notmatch '^(LeftMouseUpAction|Command1)=') { continue }
    $v = $line.Substring($line.IndexOf('=') + 1).Trim()
    $q = ([regex]::Matches($v, '"')).Count
    $o = ([regex]::Matches($v, '\(')).Count
    $c = ([regex]::Matches($v, '\)')).Count
    if ($q % 2 -or $o -ne $c) { "!! $line" }
}
```

另外：用 PowerShell 生成 ini 时，**`$UserInput$` 要写成 `` `$UserInput`$ ``
或者用单引号字符串**，否则 `$` 会被吞掉。

⚠️ **用脚本生成 meter 时同样会犯这个错，而且更隐蔽。**
加「单位」页时，生成器里的

```powershell
W "LeftMouseUpAction=[!CommandMeasure `"Script`" `"OpenTextInput('$($r.Var)'`"]"
```

少写了一个 `)`，一次生成出 6 条坏动作。所以**生成完必须跑上面那段配平自检**，
不要相信「模板是我写的所以没问题」。

---

## 6. 缩放、留白与窗口尺寸

- 主皮肤开着 `DynamicWindowSize=1`，文字、字号变化时窗口会自动跟着收放。
- **窗口原点固定在皮肤坐标 `(0,0)`**，不是内容的最小坐标。所以只要把
  `TitleX` / `TitleY` 设成正数，左边和上边就自然留出了空白。
- 右边和下面的留白靠 `CountDown.ini` 里两个完全透明的 Shape
  （`MeterPadRight` / `MeterPadBottom`）把窗口撑开，
  对应 `Layout.inc` 的 `PadRight` / `PadBottom`。设为 0 就是紧贴内容。
  注意 `MeterPadRight` 必须紧跟在 `MeterUnit` 后面
  （`X=0R` 是相对**上一个** meter 的右边缘）。
- 单位用的是相对定位 `X=(#UnitGap# * #Scale#)R`，紧贴大号数字的右边缘 ——
  数字位数变化时整体会自动变窄。
- 大号数字的 `DaysY` 是**负值**：203 号字的行框比字形本身高出约 60px，
  想让字形顶部落进窗口内就必须让行框上沿超出窗口。改 `DaysFontSize`
  时一定要同步改 `DaysY`（`DaysY ≈ 20 − 0.296 × DaysFontSize`），
  否则数字会被裁掉。

---

## 6.5 颜色用 RainRGB4 取色器，不走滑块

「外观」页的颜色不是滑块，而是**点色块调系统取色器**：

```ini
[SwatchAccent]
Meter=Shape
Shape=Rectangle 0,0,180,52,8 | StrokeWidth 1 | Stroke Color #ColorBorder# | Fill Color #ColorAccent#
DynamicVariables=1
LeftMouseUpAction=["#@#Addons\RainRGB4.exe" "VarName=ColorAccent" "FileName=#@#Configs\Variables.inc" "RefreshConfig=#CURRENTCONFIG#"]
```

- `VarName` 要写的变量、`FileName` 写哪个文件、`RefreshConfig` 写完后刷新哪个配置。
- 取色器是独立进程，**Lua 完全不参与**，所以没法像别的控件那样顺手
  `!SetVariable ... MAIN` 同步主皮肤。
- `RefreshConfig` **只能填一个配置**。我们填面板自己，主皮肤则靠
  `Settings.lua` 的 `PushColorsToMain()`（在 `Initialize()` 里调）补一刀 ——
  面板被刷新时会重新跑 `Initialize()`，正好把颜色推过去。
- 因此**面板的 `Initialize()` 不能省这一步**，否则点完色只有面板变色、皮肤不变。
- 色块的填充色直接引用 `#变量名#` + `DynamicVariables=1`，不需要 Lua 去画；
  右边的十六进制文本由 `Settings.lua` 的 `ColorHex()` 生成
  （**纯 ASCII 输出**，见第 2 条）。
- 实测 RainRGB4 **能正确处理 UTF-16 LE + BOM 的文件**：写完编码不变、中文注释完好。

---

## 6.6 中文标题写长了往左「扩」，而不是缩字号

设计稿的标题位刚好放得下 5 个中文字（meter 排版宽度 438px），
右边 23px 就是大号数字。第 6 个字开始会钻进数字下面 —— 数字在文件里排在后面，
Z 序更高，所以是标题被盖住。

处理方式（作者指定）：**保持字号和内部对齐，超出多少就把右边整块右移多少。**

```ini
; 5 个 meter 都带上同一个位移：MeterBar / MeterCnSub / MeterDays / MeterEnTitle / MeterEnDays
X=((#BarX# + Max(0, [MeterCnText:W] / #Scale# - #TitleMaxW#)) * #Scale#)
```

要点：

- **量宽度用 `[MeterCnText:W]`**（真正的标题 meter）。
  ⚠ 别用一个 `Hidden=1` 的 meter 去量 —— **隐藏 meter 的 `W` / `H` 恒为 0**，
  这是踩过的坑。
- **`MeterCnText` 必须排在所有引用它的 meter 之前**（现在它在文件最上面）。
  Rainmeter 按文件顺序更新 meter，排在后面的话会取到上一帧的宽度，
  刷新后会有 1 帧的错位。
- `#TitleMaxW#` 是**量出来的**，不是 ink 宽度：
  ink 是 423px，但 meter 的 W 含左右边距，5 个字实测 **438**。
  标定方法：标题设回 5 个字，临时加个可见 meter 显示 `[MeterCnText:W]` 读出来。
  它和 `CnFontSize` / `CnSpacing` 绑定，改了字号要重新标定。
- 位移量在设计单位里算，所以 `#Scale#` 会约掉：`W / #Scale# - #TitleMaxW#`。
- 窗口靠 `DynamicWindowSize=1` 自动加宽；23 个字的标题会撑到 2893px。
- 默认 5 字标题下位移恒为 0 —— 实测与原设计稿 **0 像素差异**。

控制面板那边也补了 `ClipString=1`（`InputTextStyle`），
长标题在输入框里显示成省略号，不会顶出去。

---

## 7. `StringAlign` 的 X / Y 是锚点

`StringAlign` 含 `Center` 的 meter，`X` / `Y` 是**文字中心**而不是外框左上角。
规则：`锚点 = 外框左上角 + 外框尺寸 / 2`。
`Settings.ini` 顶部注释里也写了这一条。

---

## 8. 怎么按新设计稿重新对齐布局

`Layout.inc` 的数值是「渲染 → 抓图量 ink 边界 → 修正」迭代出来的，
流程可复用：

1. 用 `PrintWindow`（`PW_RENDERFULLCONTENT`）抓皮肤窗口，能保留透明背景，
   别用普通截屏工具（截到的是被遮挡的画面）；
2. 按颜色分类像素，量出每个元素的 ink 边界
   （大号数字是强调色，中文 / 英文是正文色，红条是「很长的强调色竖列」）；
3. 把 ink 边界换算回 meter 的 X / Y（每行文字在行框里都有固定的顶部留白，
   换算系数 = ink 偏移 ÷ 字号）；
4. 改 `Layout.inc`，再抓一次比对。一般 2–3 轮就能收敛到 1–2px。

---

## 9. 字体

`Layout.inc` 里三个字体变量是分开的，可以单独替换：

| 变量 | 用途 |
| --- | --- |
| `FontCn` | 中文标题 |
| `FontEn` | 英文两行 |
| `FontDays` | 大号数字（默认与 `FontEn` 同款） |

字体文件放在 `@Resources\Fonts\`，由 Rainmeter 自动加载，不需要装到系统。

> 随皮肤公开分发第三方商业字库需要自行确认授权。
> 若要换成免费替代，DIN 1451 Mittelschrift 一系的复刻度量很接近
> （实测只有约 4% 的墨水宽度差），换过去基本不用重排坐标。

**关于大号数字的 4%**：设计稿里那款 DIN 比现用字体窄约 4%
（设计稿单个数字 ink 宽 108 / 118，现用字体 112 / 123，`Bahnschrift` 更宽）。
字形墨水宽度本身改不了，所以用 `Layout.inc` 的 **`DaysSpacing=-4.32`**（负字距）
把「数字块」整体收窄 —— 两位数的数字块 ink 边界正好落到设计稿的 `x484..724`，
代价是数字之间的间隙比设计稿窄约 9px。
哪天换成真正更窄的 DIN，把 `DaysSpacing` 改回 `0`，并同步调 `UnitGap`。

---

## 10. 计时逻辑的边界

- `Common.NextTarget()` 是**唯一的**计时入口，主皮肤和面板共用，
  改动时两边会一起变。
- 匹配用的是「第一个 **≥** now 的时刻」，不是 `>`：
  这样目标时刻那一秒会显示 `0`，循环计时才看得见「到点」的瞬间。
- 逐日扫描的上限由 `MaxSearchDays()` 决定：
  年固定时只找到那一年；年留空时留足 9 年（最坏情况是等 2/29）。
- 单位取整一律 `floor`。
- 英文单复数：**`n <= 1` 用单数**，也就是 0 和 1 都不加 s（作者要求）。
  改这个规则要同时改三处：`Countdown.lua` 的 `UnitEn()` / `EnLine()`
  和 `Settings.lua` 的 `PreviewRemainEn()` —— 面板预览和主皮肤必须一致。

### 10.1 单位切换阈值是「用户设置」

三个阈值**不在 Layout.inc**，而是和别的用户设置一起放在 `Variables.inc`：

| 键 | 含义 | 范围 | 默认 |
| --- | --- | --- | --- |
| `UnitSwitchDay` | 剩余不足这么多**小时** → 改用「小时」 | 24–8760 | 72 |
| `UnitSwitchHour` | 剩余不足这么多**分钟** → 改用「分钟」 | 60–10080 | 120 |
| `UnitSwitchMinute` | 剩余不足这么多**秒** → 改用「秒」 | 60–3600 | 90 |

要点：

- `Common.PickUnit()` 用 `SKIN:GetVariable` 读，所以**从 Layout.inc 搬到
  Variables.inc 不需要改任何代码** —— 只要保证 Variables.inc 在 include 顺序里
  靠后（覆盖 Layout.inc）即可。主皮肤和面板都是这个顺序。
- **下限不能放松**：天阈值若小于 24 小时，剩余 20 小时时会算出
  `floor(20/24) = 0`，屏幕上出现「0 天」。时/分的下限同理（60 分钟 / 60 秒）。
- 三档是**级联判断**，不强制单调递增。如果用户把阈值设得互相矛盾
  （比如天阈值 24 小时、时阈值 10080 分钟），某些单位可能永远轮不到 ——
  不会报错，只是那个单位用不上。

### 10.2 控制面板是 4 页

`内容(1) / 外观(2) / 单位(3) / 高级(4)`。加「单位」页时把原来的
「高级」从 Page3 重编号成了 Page4，涉及：

- `Settings.ini` 里所有 `Group=Page3` → `Page4`，`[Page3*]` meter → `[Page4*]`
- 导航从 3 项变 4 项：新增 `NavBg4` / `Nav4`（Y=232），
  并把 `NavBg3` / `Nav3` 的文字从「高级」改成「单位」
- `Settings.lua` 的 `PAGE_COUNT` 3 → 4

新页的三个阈值字段复用了目标时间那套 `FIELDS` 机制（`StepField` /
`SetFieldValue` / `SyncFields`），只是多了个 `noWild = true`：
不允许留空，值不合法就显示兜底值 `def`，这样输入框清空也不会变成「循环」。

---

## 10.5 为什么空白处点不动：透明像素不接收鼠标

Rainmeter 皮肤是 **分层窗口（layered window）**，逐像素 alpha。
**alpha = 0 的像素会被系统判定为「点不到」**，鼠标事件直接穿过去，
于是只有文字笔画、色块这些有实际像素的地方能按住拖动皮肤。

解决方法是铺一层 alpha 极低（1/255）但不为 0 的遮罩：

```ini
[MeterDragLayer]
Meter=Shape
Shape=Rectangle 0,0,([MeterPadRight:X] + [MeterPadRight:W]),([MeterPadBottom:Y] + [MeterPadBottom:H]) | StrokeWidth 0 | Fill Color 0,0,0,1
X=0
Y=0
DynamicVariables=1
```

要点：

- **alpha 必须是 1，不能是 0**。1/255 肉眼完全看不出来，但足以让窗口接收鼠标。
  下面 MeterPadRight / MeterPadBottom 早就在用同一个招数。
- **给 7 个可见 meter 各铺一块矩形**，而不是整窗一块大矩形 ——
  后者会把红条左边、数字右下、英文右侧那些空档也变成可拖动。
  每块用 section variable 取对应 meter 的 `X / Y / W / H`，
  所以字号、缩放、标题长度变化时都自动跟着走：

  ```ini
  Shape =Rectangle ([MeterCnText:X]),([MeterCnText:Y]),([MeterCnText:W]),([MeterCnText:H]) | ...
  Shape2=Rectangle ([MeterCnSub:X]),([MeterCnSub:Y]),([MeterCnSub:W]),([MeterCnSub:H]) | ...
  ; 共 7 块：CnText / CnSub / Bar / Days / Unit / EnTitle / EnDays
  ```

- 可拖动范围 = 这些 meter 的**行框**，不是笔画本身。
  中文标题的行框高 100px 而字形只有约 78px —— 行框内的空白同样能拖。
  想再收紧只能「把文字用 alpha=1 再画一遍」，代价是 7 个重复 meter
  要跟着布局一起维护，收益不大。
- ⚠ **不要在 Shape 的 `Rectangle` 里写 `Max(a,b)`** ——
  Rectangle 用逗号分隔参数，函数里的逗号会被当成多出来的参数，
  实测会让那块矩形直接失效。要取较大值就在别处算好再传进来。
- **必须放在所有被引用的 meter 之后**：Rainmeter 按文件顺序更新 meter，
  放前面会读到上一帧的值，而 `Update=1000` 意味着一帧 = 1 秒，会明显闪。
- 盖在最上层不会挡住东西 —— 主皮肤里没有任何 meter 带鼠标动作，
  右键菜单来自 `[Rainmeter]` 段的 `ContextAction`，属于整个皮肤。
- 验证方法：`GetWindowRect` 记下窗口位置 → 在空白处 `mouse_event` 按下并移动
  → 再看位置有没有变。窗口贴到屏幕边缘时位移会被截断，属正常。
- 面板不需要它：`[Background]` 本来就是 760x560 的不透明 Shape。

---

## 11. 仓库：用 working-tree-encoding 存 UTF-16 文件

远程仓库 <https://github.com/xiaochengzina/TheWanderingEarth2Countdown>

`.ini` / `.inc` 必须是 UTF-16 LE + BOM，但如果**原样存进 git**，
GitHub 网页上会显示成 `Binary file not shown`，没法 review、没法 diff。
所以 `.gitattributes` 里用了 `working-tree-encoding`：

```gitattributes
*.ini  text eol=crlf working-tree-encoding=UTF-16LE-BOM
*.inc  text eol=crlf working-tree-encoding=UTF-16LE-BOM
*.exe  binary
```

| 位置 | 实际编码 |
| --- | --- |
| 工作区（磁盘上） | UTF-16 LE + BOM + CRLF ← Rainmeter 读的 |
| 仓库里（blob） | UTF-8 + LF ← GitHub 上能正常显示 diff |
| 检出到新机器 | 由 git 自动转回 UTF-16 LE + BOM + CRLF |

要点与坑：

- **需要 git >= 2.18**。不认这个属性的客户端检出后会拿到 UTF-8，皮肤会变成一片空白；
  手工另存回 UTF-16 LE + BOM 即可救回。
- 改完属性必须 `git add --renormalize .` 重新编码已存的内容，
  否则老 blob 还是二进制。
- 想强制刷新工作区（比如行尾规则改了）：删掉文件再 `git checkout -- .`，
  git 会按**当前**属性重新生成。
- 验证套路：`git clone` 到临时目录，然后**逐字节比对**哈希。
  克隆出来应当与工作区完全一致（字体除外，它被 `.gitignore` 排除）。
- 字体不进仓库（第三方商业字库），但 `@Resources/Addons/RainRGB4.exe`
  是入库的 —— 与 `AmphoreusCalendar` 仓库的做法保持一致。

---

## 12. 已知限制与「看着像 bug 其实不是」

按严重程度排列。带 ✅ 的已经修了，带 ⚠️ 的是平台限制或待定项。

| # | 现象 | 结论 |
| --- | --- | --- |
| 0 | ✅✅ **年份很远时每秒崩溃刷屏**：`Script: Common.lua:170: attempt to compare nil with number` | **最严重的一条**。`os.time{year=9999,...}` 在 Windows 上返回 **nil**（C 运行时的 64 位时间上限在 3001–3100 之间），然后 `nil < now` 直接抛错，而且 `FindTimeOnDay` 每秒都会被调用一次 → 日志每秒刷一条 ERRO。**这条是「性能优化」引出来的**：原来要空转约 290 万次才慢慢撞到 9999，加上「跳到目标年」的快速路径后变成立刻崩。修法两层：① `FindTimeOnDay` / `DaysLeft` 里每个 `os.time` 结果都判 nil；② 新增 `YEAR_MAX = 3000` 常量，`TargetSpec` 与面板 FIELDS 的年份上限都从 9999 降到它。**注意手改 `Variables.inc` 能绕过面板上限，所以第①层不能省。** |
| 1 | ✅ 单位阈值滑块在最小值按「−」会跳到最大值 | `StepField` 原本对所有字段都绕圈。日期字段（秒 59→0、年 9999→1970）绕圈合理，阈值字段不合理 → 给这三个字段加了 `clamp = true`，到端点停住 |
| 2 | ✅ 清空「秒」会写入空值，面板还把它显示成「循环」 | `Sec` 字段缺 `noWild` / `def`。补上 `noWild = true, def = 0`，并让 `SetFieldValue` 遇到 `noWild` 字段时落回 `f.def` 而不是写空（否则 SyncFields 里 `math.floor(nil)` 会直接报错） |
| 3 | ✅ 手改 `UnitSwitchDay=0` 后剩余 20 小时显示成「0 天」 | 阈值 0 时 `rem >= 0` 恒成立，永远用天。`Common.PickUnit` 里对三个阈值加了 `math.max(下限, …)` 兜底 |
| 4 | ✅ 目标年很远（如 9999）且月/日留空时皮肤卡顿 | `NextTarget` 从今天逐日往后挪，`(9999-2026)*366` ≈ 290 万次空转，而每秒都调一次。改成「年固定且在将来时直接跳到那年 1 月 1 日」，最多扫 366 天。**但这一改把第 0 条的崩溃从「慢慢撞上」变成「立刻崩」—— 两条必须一起改，单独改性能那条是有害的。** |
| 5 | ✅ 面板每次加载/刷新都重写 `Variables.inc` | `ShowPage` 无条件调用 `Common.WriteVar` 写 `SettingsPage`，导致这个受版本管理的文件永远是「已修改」。改成页码真的变了才写 |
| 6 | ⚠️ 标题里输入英文双引号 `"` 会失效（日志报 `!CommandMeasure: Skin ... does not exist`） | **Rainmeter 平台限制**：`$UserInput$` 是文本替换进 action 字符串的，值里带 `"` 必然破坏外层引号。皮肤层无法转义。表现为「静默不生效 + 日志一行 ERRO」，不会写坏文件。**已在第 1 页底部加提示**（`Page1InputHint`），README 常见问题里也写了一条 |
| 7 | ⚠️ 输入框清空「中文标题 / 英文标题」后不生效 | `SetTextValue` 里有一句 `if value == '' then return end` 直接丢弃空值。行为本身可辩（空标题是合法外观），但**静默无反馈**容易让人以为坏了。**已按作者要求在第 1 页底部加两行提示说明这两点**（`Page1InputHint` / `Page1InputHint2`），行为本身保持不动 |
| 8 | ⚠️ 面板「外观」页底部约 250px 空白 | 页面只到 y≈310，而 1/3/4 页都排到 450–500。待定 |

### 独立审查的结论（用于以后判断要不要再花这个成本）

派出去的三路子代理里，只有第三路（严格只读）交了报告。它列了 3 条「错误输出」，
**经实测全部是假阳性**：

| 它报的 | 实测 |
| --- | --- |
| `FindTimeOnDay` 的时/分留空时只试 00:00，会差约 24 小时甚至永远显示 0 | **假阳性**。`hFrom,hTo = 0,23` / `miFrom,miTo = 0,59` 本来就是留空时的完整范围，循环会遍历全部 1440 个分钟。实测「全留空 + 秒=30」显示「37 秒」，正确 |
| 不存在的月/日（2/30）会永远 0 | **是设计如此**（无法命中的目标就停在 0），不是 bug |
| `cache.value` 会读到过期的 0 | **假阳性**。`cache.at` 与 `unit`/`value` 是**同一次赋值**写入的，命中 `cache.at == now` 说明它们本来就是这一秒的值；`ForceRender` 把 `at` 置 -1 之后下次调用会三个一起重算 |

**它没发现的，反而是最严重的第 0 条崩溃。**

**结论：这套代码光靠「读代码」审查，收益很低、假阳性很高。**
真正抓到 bug 的是**行为测试**：构造边界输入 → 跑一遍 → 看 `Rainmeter.log` 有没有 ERRO。
以后审查优先做这个，读代码只作为补充。

### 审查时踩的坑（工具层面）

- 派出去做只读审查的子代理**擅自往 `Settings.ini` 的 `[Variables]` 段中间插入了自己的测试段**，
  段头把变量段提前截断，`FontMain` / `ColorBg` 等全被并进测试段 → 面板背景变白。
  已用 `git checkout -- Settings/Settings.ini` 恢复，并删掉 `auditprobe.lua` / `auditscratch.inc`。
  **教训**：让子代理审查时必须在提示词里写明「禁止创建/修改/删除任何文件」，
  并且审查前后对全项目做哈希快照比对。
- 主皮肤的 `Variables.inc` 是**活文件**，作者随时可能在用皮肤时改它。
  测试要临时改设置时，先 `Copy-Item` 备份、测完原样还原，
  并且**只改自己要测的键**，不要整文件覆写。

---

## 13. 打包发布（.rmskin）

### ⚠️ 不能自己造 .rmskin

Rainmeter 的 .rmskin **不是普通 zip**：它带一份由 Skin Packager 写入、安装前会校验的
完整性元数据。官方文档（https://docs.rainmeter.net/manual/distributing-skins/ ）原话：

> As of version 2.3, Rainmeter will not install a normal ZIP file changed to the ".rmskin"
> extension; the package must be created with the Skin Packager.

**踩过的坑（1.2.0 发布时）**：为了去掉包里的 `开发笔记.md` / `.gitattributes` / `.gitignore`，
我用 .NET 的 `ZipFile` 把原包解开、删掉三个条目、再重新压回去。内容逐字节都对、
`RMSKIN.ini` 也是原样，**但装的时候直接报错** —— 重建的包没有 Skin Packager 的签名。

**教训：发布前先查官方文档，不要凭「zip 而已」就动手。**
对外的坏包已删掉资产并转成 Draft（`gh release edit v1.2.0 --draft`），标签保留。

### ✅ 排除开发文件的正解：设成隐藏

同一篇文档给了官方做法：

> The Skin Packager will ignore any hidden files or folders in your root config folder.
> You can take advantage of this behavior by storing development files ... as hidden files,
> so that they are excluded from the final product.

```powershell
# 在皮肤根目录执行；Skin Packager 打包时会自动跳过这些文件
foreach ($n in '.gitattributes','.gitignore','开发笔记.md') {
    $x = Get-Item $n -Force
    $x.Attributes = $x.Attributes -bor [System.IO.FileAttributes]::Hidden
}
```

`@Resources\Configs\Variables.inc` **绝对不能隐藏** —— 它是皮肤设置，必须进包。

> ⚠️ **隐藏文件的副作用**：`.NET` 的 `File.WriteAllLines` / `WriteAllText` 写**隐藏文件**
> 会直接抛 `UnauthorizedAccessException: Access to the path ... is denied`。
> 已用对照实验确认（同一文件，摘掉 Hidden 后连写 5 次全成功，加上 Hidden 后连写 5 次全失败）。
> 所以脚本要改 `开发笔记.md` 时，**先把 Hidden 摘掉，写完再设回去**：
>
> ```powershell
> $i = Get-Item $f -Force
> $i.Attributes = $i.Attributes -band (-bnot [System.IO.FileAttributes]::Hidden)
> # ...写文件...
> $i = Get-Item $f -Force
> $i.Attributes = $i.Attributes -bor [System.IO.FileAttributes]::Hidden
> ```
>
> 编辑器（VS Code / 记事本）一般不受影响，它们通常用「临时文件 + 改名」的方式保存。

### 打包步骤（Skin Packager）

1. 右键 Rainmeter 托盘图标 → **管理** → 左下角 **Create .rmskin package...**
2. Name / Author / Version 填 `TheWanderingEarth2Countdown` / `小城子` / `1.2.0`
3. **Add skin...** 选皮肤根目录（可以指向任意位置，不必在 Skins 下）
4. Next → 勾 **Load skin** 选 `CountDown.ini`；Minimum Rainmeter `4.0`、Windows `10.0`
5. Create package

### 外层 zip（「完整套装」）

把 `.rmskin` 和 `Rainmeter-4.5.26.exe` 一起压成一个 zip 即可，用户解压后双击 .rmskin。

### GitHub 发布

```powershell
gh release create v1.2.0 <zip> <rmskin> --repo xiaochengzina/TheWanderingEarth2Countdown --title "..." --notes-file <说明.md>
```

- 发布说明写成文件再用 `--notes-file`：直接写在命令行里，中文 + 引号会被 PowerShell 拆坏。
- 判断提交/推送是否成功要**比对操作前后的 commit hash**；只看 `local == remote` 会误报。
- 上传后把资产下载回来比哈希，并核对 GitHub 自己算的 `digest`。

---

## 14. 面板页码与「改完要刷新」

### SettingsPage 的语义：当前页 + 关闭时归位

需求变过三次，最后定下来的分工是：

| 时机 | 行为 | 靠什么 |
| --- | --- | --- |
| 面板开着时翻页 / 取色刷新 | 停在你正在看的页 | `ShowPage` 记住当前页（值变了才写文件） |
| 关闭面板 | 页码写回 1 | `ClosePanel()`（关闭按钮调它，再 `!DeactivateConfig`） |
| 打开面板 | 第 1 页（内容） | 上一行保证了 |
| 点「恢复默认设置」 | 第 1 页（内容） | `ResetToDefaults` 末尾 `ShowPage(1)` |

**为什么不能「永远回首页」**：取色器 RainRGB4 改完颜色会 `!Refresh` 面板自己
（色块要重新读 `Variables.inc` 才会变色），刷新会重跑 `Initialize`。
`Initialize` 里永远 `ShowPage(1)` 的话，用户挑完颜色就被弹回首页 ——
「改颜色」这个动作等于把人赶走。这是实际踩过的回归。

### ⚠️ 配色是「加载时解析」的，改了变量不等于界面上会变

Rainmeter 里 `#ColorAccent#` 出现在**Shape 的 Fill Color / Stroke Color**、
以及**没有 `DynamicVariables=1` 的 meter 属性**里时，是在**加载皮肤那一刻**
解析的。之后 `!SetVariable` + `!Redraw` 都不会让它变。

所以「恢复默认设置」光写变量 + 重绘是不够的：面板里页标题、滑块轨道、
色块都会停在旧配色上。**必须 `!Refresh` 一次**，让它从刚写好的
`Variables.inc` 完整重读。`ResetToDefaults` 末尾就是这么做的。

对比之下**主皮肤不需要刷新**：它的配色走 `!SetVariable ... MAIN`
外加 `ForceRender()`（连做两次 `!UpdateMeter *` + `!Redraw`），实测颜色能跟上。
差异在于主皮肤那几个 Shape meter 带了 `DynamicVariables=1`。

### 面板自己的配色会被用户配色覆盖

`Settings.ini` 的 `[Variables]` 里定义了面板自己的 `ColorAccent` / `ColorText`
（深色 + 红），但后面又 `@include3=#@#Configs\Variables.inc`，用户的同名变量
把面板的覆盖掉了。结果是**用户把正文色挑成绿色，面板自己的页标题也变绿**。
目前保持现状（未处理）—— 想固定面板外观就得把这几个变量改名，
比如 `UiAccent` / `UiText`，只让色块读用户颜色。
